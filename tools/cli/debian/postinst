#!/bin/sh
# postinst script for netidm
#
# see: dh_installdeb(1)

set -e

case "$1" in
    configure)
        if [ ! -f /etc/netidm/config ]; then
            echo "============================="
            echo "Thanks for installing Netidm!"
            echo "============================="
            echo "Please ensure you create a configuration file at /etc/netidm/config or ~/.config/netidm. An example file is located in /usr/share/netidm/"
        fi
    ;;

    abort-upgrade|abort-remove|abort-deconfigure)
    ;;

    *)
        echo "postinst called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
