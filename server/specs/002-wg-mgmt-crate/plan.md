# Implementation Plan: WireGuard Management Crate

**Branch**: `002-wg-mgmt-crate` | **Date**: 2026-04-17 | **Spec**: [spec.md](spec.md)

## Summary

Add a `server/wg/` Rust crate to netidm that manages live WireGuard interfaces using the DL16 `WgTunnel`/`WgPeer` schema as its data store. At startup the daemon probes `/sys/module/wireguard`; if present it uses wireguard-control with the kernel backend, otherwise it embeds a boringtun userspace device. A new DL17 migration adds `WgToken` (registration token entry class) and `WgLastSeen` attribute. HTTP handlers in `server/core` expose tunnel/peer/token CRUD and a public `POST /v1/wg/connect` client endpoint. The Go wgdb (`~/wgdb/go/`) is the behavioral reference.

## Technical Context

**Language/Version**: Rust stable (see `rust-toolchain.toml`)  
**Primary Dependencies**:
- `wireguard-control` — WireGuard device configuration (kernel netlink + UAPI)
- `boringtun` — userspace WireGuard protocol implementation (device module with UAPI socket)
- `tun2` — TUN device creation on Linux
- `rtnetlink` — async netlink for link/address/route management
- `ipnet` — CIDR parsing and slot-based IP allocation
- `utoipa` (workspace) — OpenAPI annotations on handlers and types

**Storage**: Netidm entry database (netidmd_lib QueryServer)  
**Testing**: `cargo test` — unit tests for IP allocator and token validation; integration tests require `CAP_NET_ADMIN` and are gated behind a feature flag  
**Target Platform**: Linux (kernel WireGuard or boringtun userspace). macOS/Windows out of scope for this iteration.  
**Performance Goals**: Tunnel bring-up < 5s; peer registration < 3s; handshake poll cycle ≤ 30s  
**Constraints**: No `#[allow(...)]` suppressions; `cargo clippy -- -D warnings` must pass; `git clone && cargo test` must pass without external services (network-dependent tests are `#[cfg(feature = "integration")]`)

## Constitution Check

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Ethics & Human Rights | ✅ Pass | WgPeer entries linked to Person accounts; users can request deletion of their peer entries |
| II. Humans First | ✅ Pass | Registration flow is self-service (token + pubkey → config); no manual admin intervention for clients |
| III. Correct & Simple | ✅ Pass | `cargo test` passes without external services; network tests gated behind feature flag |
| IV. Code Quality (Clippy) | ✅ Pass | All new code must pass `cargo clippy -- -D warnings`; no `#[allow(...)]` |
| V. Security by Hierarchy | ✅ Pass | Token secrets hashed at rest; private keys in Utf8String (admin-readable via ACP only); hook commands sanitised at write time |

**Gate**: PASS — no violations.

## Project Structure

### Documentation (this feature)

```text
specs/002-wg-mgmt-crate/
├── plan.md              ← this file
├── research.md          ← Phase 0 decisions
├── data-model.md        ← entity definitions
├── quickstart.md        ← usage scenarios
├── contracts/
│   └── wg-api.md        ← HTTP API contract
└── tasks.md             ← generated by /speckit-tasks
```

### Source Code

```text
server/wg/                          ← new crate
├── Cargo.toml
└── src/
    ├── lib.rs                      ← crate root; re-exports WgManager, detect_backend
    ├── backend/
    │   ├── mod.rs                  ← WgBackend trait definition
    │   ├── kernel.rs               ← KernelBackend (wireguard-control + rtnetlink)
    │   └── boringtun.rs            ← BoringtunBackend (boringtun device + tun2 + wireguard-control)
    ├── manager.rs                  ← WgManager: lifecycle orchestration, poller
    ├── alloc.rs                    ← IpAllocator: slot-based CIDR allocation
    ├── hooks.rs                    ← pre/post hook execution
    └── types.rs                    ← shared structs (WgTunnelConfig, WgPeerConfig)

server/lib/src/
├── migration_data/dl17/
│   ├── mod.rs                      ← phase_1_schema_attrs, phase_2_schema_classes
│   └── schema.rs                   ← SCHEMA_ATTR_WG_LAST_SEEN, SCHEMA_ATTR_WG_TOKEN_*,
│                                     SCHEMA_CLASS_WG_TOKEN
├── constants/uuids.rs              ← new UUIDs for DL17 attrs/classes
├── constants/mod.rs                ← EntryClass::WgToken, Attribute::WgLastSeen etc.
└── idm/
    └── wg.rs                       ← business logic: token validation, peer provisioning,
                                      IP allocation via IpAllocator, last_seen write-back

server/core/src/
├── https/
│   ├── v1_wg.rs                    ← Axum handlers (tunnel/peer/token CRUD + /v1/wg/connect)
│   ├── v1.rs                       ← route registration for /v1/wg/* routes
│   └── apidocs/mod.rs              ← add v1_wg handlers + WG types to ApiDoc
└── actors/
    └── v1_write.rs                 ← handle_wg_* methods (tunnel CRUD, peer provision,
                                      token create/consume, last_seen update)

tools/cli/src/
├── cli/wg.rs                       ← CLI command dispatch for wg subcommands
└── opt/netidm.rs                   ← WgOpt enum: tunnel/peer/token subcommands

proto/src/
└── wg.rs                           ← WgTunnelCreate, WgConnectRequest, WgConnectResponse,
                                      WgPeerResponse, WgTokenCreate etc. (ToSchema)
```

## WgBackend Trait

```rust
// server/wg/src/backend/mod.rs
#[async_trait]
pub trait WgBackend: Send + Sync {
    async fn create_link(&self, name: &str, mtu: Option<u32>) -> Result<()>;
    async fn delete_link(&self, name: &str) -> Result<()>;
    async fn configure(&self, name: &str, cfg: &WgTunnelConfig, peers: &[WgPeerConfig]) -> Result<()>;
    async fn add_peer(&self, name: &str, peer: &WgPeerConfig) -> Result<()>;
    async fn remove_peer(&self, name: &str, pubkey: &str) -> Result<()>;
    async fn peer_handshakes(&self, name: &str) -> Result<HashMap<String, i64>>;
}

pub async fn detect_backend() -> Box<dyn WgBackend> {
    if std::path::Path::new("/sys/module/wireguard").exists() {
        Box::new(KernelBackend::new().await)
    } else {
        Box::new(BoringtunBackend::new().await)
    }
}
```

## HTTP API Routes

New routes added to `server/core/src/https/v1.rs`:

```
GET    /v1/wg/tunnel                       → wg_tunnel_get
POST   /v1/wg/tunnel                       → wg_tunnel_post
GET    /v1/wg/tunnel/{name}                → wg_tunnel_id_get
PATCH  /v1/wg/tunnel/{name}                → wg_tunnel_id_patch
DELETE /v1/wg/tunnel/{name}                → wg_tunnel_id_delete

POST   /v1/wg/tunnel/{name}/token          → wg_token_post
GET    /v1/wg/tunnel/{name}/token          → wg_token_get
DELETE /v1/wg/tunnel/{name}/token/{token}  → wg_token_id_delete

GET    /v1/wg/tunnel/{name}/peer           → wg_peer_get
DELETE /v1/wg/tunnel/{name}/peer/{peer}    → wg_peer_id_delete

POST   /v1/wg/connect                      → wg_connect (public — token auth only)
```

## DL17 Schema Additions

New schema attrs:

| Attribute | Syntax | Notes |
|-----------|--------|-------|
| WgLastSeen | Utf8String | RFC3339 timestamp; written by daemon poller |
| WgTokenSecret | Utf8String | Hashed token secret |
| WgTokenUsesLeft | Uint32 | Absent = unlimited |
| WgTokenExpiry | Utf8String | RFC3339 expiry |
| WgTokenPrincipalRef | ReferenceUuid | Optional: lock to one user |

New class: `WgToken`

| systemmust | systemmay |
|------------|-----------|
| Name, WgTunnelRef, WgTokenSecret | WgTokenUsesLeft, WgTokenExpiry, WgTokenPrincipalRef |

`WgLastSeen` added to `WgPeer.systemmay`.

## Background Tasks (netidmd startup)

Two tokio tasks spawned after tunnel bring-up:

1. **`wg_handshake_poller`** — every 30s: calls `backend.peer_handshakes(name)` for each live tunnel, writes `WgLastSeen` to each WgPeer entry via actor write path.

2. **`wg_peer_watch`** — every 30s: queries Netidm for WgPeer entries referencing each tunnel, diffs against the set known to the live interface, calls `backend.remove_peer()` for any that have been deleted.

## IP Allocation (server/wg/src/alloc.rs)

Slot-based allocation matching wgdb Go reference:
- Slot 1 = server address (reserved; taken from `WgAddress` entry)  
- Slots 2+ = assigned to peers in order
- On registration: scan existing `WgAllowedIps` values across all peers of the tunnel; find lowest unused slot; assign as `/32` (IPv4) or `/128` (IPv6)
- IPv4 and IPv6 dual-stack: if tunnel has both, peer gets one slot in each space

## Token Flow (POST /v1/wg/connect)

1. Look up `WgToken` entries where `WgTokenSecret` matches hash of provided secret
2. Check: not expired, `WgTokenUsesLeft` > 0 (or absent), `WgTokenPrincipalRef` matches caller (or absent)
3. Check: provided pubkey not already registered on this tunnel (unique constraint on `WgPubkey`)
4. Allocate IPs via `IpAllocator`
5. Create `WgPeer` entry in Netidm (Name, WgPubkey, WgAllowedIps, WgTunnelRef, WgUserRef)
6. Decrement `WgTokenUsesLeft`; if reaches 0, delete `WgToken` entry
7. Hot-add peer to live interface via `backend.add_peer()`
8. Return `WgConnectResponse` with serialised wg-quick config string

## Complexity Tracking

No constitution violations.

## Open Questions (resolved)

- **Webhook?** No — `POST /v1/wg/connect` is a synchronous REST endpoint, not a callback.
- **OpenAPI?** Use existing `utoipa` + `utoipa-swagger-ui` setup; register in `apidocs/mod.rs`.
- **Token scope**: `WgToken` is always user-scoped — creating a peer requires a Netidm authenticated session OR the token must carry `WgTokenPrincipalRef`. The `/v1/wg/connect` endpoint requires an authenticated Netidm session (bearer token) in addition to the WG token secret, so the resulting `WgPeer.WgUserRef` is always set.
